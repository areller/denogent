# automatically generated by denogent

name: denogent-build
'on':
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    container: 'ubuntu:latest'
    services:
      redis:
        image: redis
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: setup deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: v1.4.1
      - name: run build
        run: >-
          deno run -A -q --unstable build/build.ts --serial --ci-runtime
          github_actions run
        env:
          REDIS_HOST: redis
          REDIS_PORT: '6379'
          REDIS_PORTS: '6379'
