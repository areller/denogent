# automatically generated by denogent

name: denogent-build
'on':
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: setup deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: v1.4.1
      - name: Install NodeJS latest
        uses: actions/setup-node@v1
      - name: run build
        run: >-
          deno run -A -q --unstable build/build.ts run --runtime gh-actions
          --serial --only "npm install" --only "lint"
  build-linux:
    name: build-linux
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: setup deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: v1.4.1
      - name: run build
        run: >-
          deno run -A -q --unstable build/build.ts run --runtime gh-actions
          --serial --only "unit tests" --only "e2e tests" --only "test"
  build-windows:
    name: build-windows
    runs-on: windows-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: setup deno
        uses: denolib/setup-deno@v2
        with:
          deno-version: v1.4.1
      - name: run build
        run: >-
          deno run -A -q --unstable build\build.ts run --runtime gh-actions
          --serial --only "unit tests" --only "e2e tests" --only "test"
